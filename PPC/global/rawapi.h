/* output a int value to simulator's console. */
extern void * memset(void * s, int c, int n);
extern unsigned char * strncpy(unsigned char *dest, const unsigned char*src, int n);
extern int isspace (int c);
